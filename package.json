{
  "name": "file-organizer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\"",
    "tauri": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@tauri-apps/cli": "^2.0.0"
  }
}